import React from "react";
import FormAsli from "./source/form";

export default class Form extends React.Component{
    render(){
        return(
            <div>
                <FormAsli />
            </div>
        )
    }
}